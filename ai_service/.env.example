# JWT Configuration
JWT_ALGORITHM=RS256
JWT_PUBLIC_KEY_PATH=./jwt_public_key.pem

# CORS Configuration
# In production, set this to the backend's internal URL (e.g. http://backend:3000) to only accept requests from the backend service.
ALLOWED_ORIGINS=http://localhost:3000
